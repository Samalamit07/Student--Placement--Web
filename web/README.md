# Web — Student Management & Placement System

React.js (Vite) web application for Admins and Placement Officers.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

The app expects the FastAPI backend running at the URL set in `VITE_API_URL`.
